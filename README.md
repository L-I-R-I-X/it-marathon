# Цифровой марафон 2026 | Проектный этап | Исследователи

Привет, участник Цифрового марафона!

Рады видеть тебя на Проектном этапе! Здесь тебе предстоит создать  веб-приложение, выполнив три задания. Напоминаем, что этот этап является обязательным для прохождения! Не переживай, мы создали все условия, чтобы тебе было комфортно творить.

Вот как это работает:
1. Этап открыт с 06.04.26 по 20.04.26. Ты можешь сдать решение в любой момент в этом интервале — выбирай удобное время.
2. Мы не ограничиваем твой полет мысли! Выбирай любой язык программирования, который позволит решить поставленную задачу с теми ограничениями, что мы в ней предусмотрели! Более того, ты можешь смело использовать большие языковые модели (мы это приветствуем) как своих помощников.
3. Твои решения будет проверять автоматическая система. Все нюансы оценки расписаны в п. 5.10 [Положения о конкурсе](https://it-marathon.21-school.ru/clause.pdf), чтобы для тебя не было сюрпризов.
4. Как сдать решение: когда все задачи будут решены, перейди в карточку этапа в Личном кабинете, прикрепи ссылку на свой репозиторий, нажми кнопку «Валидировать решение», а затем, если при валидации никаких проблем обнаружено не было, нажми «Сдать задание».
5. Важный момент: сдать задание можно только один раз, поэтому перед отправкой внимательно проверь свое решение.
6. Результаты проверки и начисленные баллы будут отображены в [Личном кабинете](https://it-marathon.21-school.ru/stages) после завершения Проектного этапа и проверки решений всех участников в целях предотвращения недобросовестных практик.

Подробнее с требованиями можно ознакомиться в разделе [«Технические требования»](#технические-требования-для-всех-задач).

Что запрещено (и повлечет за собой немедленную дисквалификацию):
1. Совершать любые хулиганские или противоправные действия (в том числе использовать ненормативную лексику в комментариях в репозитории) согласно п. 9.6.1 [Положения о конкурсе](https://it-marathon.21-school.ru/clause.pdf).
2. Выполнять задания с посторонней помощью. Если в твоем репозитории будут замечены другие соавторы (кроме нашего проверяющего) или параллельные коммиты с разных аккаунтов, мы будем вынуждены тебя дисквалифицировать согласно п. 9.5.5 [Положения о конкурсе](https://it-marathon.21-school.ru/clause.pdf).
3. Выкладывать задания или решения в открытый доступ согласно п. 9.5.9 [Положения о конкурсе](https://it-marathon.21-school.ru/clause.pdf).
4. Добавлять вредоносный код в свое решение. Ты несешь ответственность за тот код и содержимое образа, которые ты направляешь на проверку, в соответствии с [Положением о конкурсе](https://it-marathon.21-school.ru/clause.pdf), и нарушение (в том числе неправомерные действия) приведет к дисквалификации.
Цифровой марафон нацелен на то, чтобы помочь тебе повысить цифровые навыки в части отечественных технологий. GitVerse — это удобно, надежно и совсем не страшно!

Лайфхак: если хочешь быстро разобраться с платформой, сначала загляни в наш демо-тренажер. Его можно найти на карточке этапа. Там мы показали, как работать на GitVerse без лишних сложностей. Если у тебя останутся вопросы, можно заглянуть в [официальную документацию GitVerse](https://gitverse.ru/docs/gitverse/).

> Помни, что решение должно соответствовать [Техническим требованиям](#технические-требования-для-всех-задач)

Удачи в разработке!


## Содержание

- [Твоя миссия](#твоя-миссия)
  - [Требования к решению](#требования-к-решению)
- [Задача 1: «Круиз Робинсона»](#задача-1-круиз-робинсона)
  - [Легенда](#легенда)
  - [Что мы знаем:](#что-мы-знаем)
  - [Гравитационный маневр](#гравитационный-маневр)
  - [Что нужно сделать в этой задаче](#что-нужно-сделать-в-этой-задаче)
  - [API-интерфейс задачи](#api-интерфейс-задачи)
- [Задача 2: «Гори, гори, моя звезда»](#задача-2-гори-гори-моя-звезда)
  - [Легенда](#легенда-1)
  - [Что мы знаем:](#что-мы-знаем-1)
  - [Звездная система](#звездная-система)
  - [Что нужно сделать в этой задаче](#что-нужно-сделать-в-этой-задаче-1)
  - [API-интерфейс задачи](#api-интерфейс-задачи-1)
- [Задача 3: «Звездочетность»](#задача-3-звездочетность)
  - [Легенда](#легенда-2)
  - [Что мы знаем](#что-мы-знаем-2)
  - [Что нужно сделать в этой задаче](#что-нужно-сделать-в-этой-задаче-2)
  - [API-интерфейс задачи](#api-интерфейс-задачи-2)
- [Технические требования для всех задач](#технические-требования-для-всех-задач)


# Твоя миссия
Ты — научный сотрудник на борту научно-исследовательского корабля, потерпевшего крушение на неизвестной планете. Пока техники чинят двигатели, от тебя зависит спасение миссии.
Твоя задача — разработать единое веб-приложение, которое решит три ключевые научные задачи, стоящие перед экипажем. 
## Требования к решению
Тебе нужно создать веб-приложение, которое:
- запускает HTTP-сервер на порту 8000;
- предоставляет три API-эндпоинта (по одному для каждой задачи);
- принимает входные данные в формате JSON через POST-запросы;
- выполняет расчеты с учетом всех условий и ограничений;
- возвращает результаты в строго определенном JSON-формате.

Теперь перейдем к конкретным задачам. Каждая из них представляет отдельную научную проблему, которую необходимо решить для спасения экипажа.

# Задача 1: «Круиз Робинсона»
## Легенда
Корабль поврежден, но у нас есть аварийный шаттл, отчаянный агент Робинсон, готовый лететь за помощью, и мы как раз обнаружили планету, где мы сможем договориться о проведении спасательной экспедиции. Туда нам и надо отправиться. Проблема в том, что взлёт с нашей текущей планеты (назовем ее ```start_point```) требует чудовищных затрат топлива — настолько огромных, что каждый лишний килограмм на орбите стоит целого бака горючего.

Твоя задача – найти максимально оптимальный (по времени и топливу) путь до целевой планеты (назовем ее ```rescue_point```). 
P.s. запомни имена планет (начальной и целевой), они тебе пригодятся!
## Что мы знаем:
- на вывод единицы массы на орбиту нужно огромное количество топлива (считаем, что человек и баки с запасом кислорода массы не имеют);
- на борту ограничен запас воздуха — полет не может длиться вечно;
- после выхода на орбиту шаттл летит по автопрограмме:
  - половина имеющегося на борту топлива сразу тратится на разгон (с округлением до целых вниз);
    - при этом разгон принудительно прекращается, если при сжигании очередной единицы топлива будет пройдено больше половины от всего запланированного расстояния;
    - пока разгон не завершен, выполнять гравитационные маневры нельзя (но можно просто перемещаться по ребрам графа)
  - затем шаттл летит по инерции (равномерное движение без ускорений); при этом можно заложить в программу выполнение одного или нескольких гравитационных маневров, на которые будет тратиться время и топливо;
  - при приближении к цели оставшаяся часть топлива тратится на полное торможение шаттла, т.е. полет должен закончиться с нулевой скоростью
    - учитывай, что если мы взяли гравитационный разгон, то тормозить, соответственно, придется дольше; аналогично разгону, в процессе торможения никакие маневры выполнять нельзя;
  - работа двигателей на разгон и торможение осуществляется таким образом, что каждую секунду сжигается одна единица топлива:
    - учитывай, что чем меньше на борту топлива, тем меньше суммарная масса шаттла, следовательно, тем большее ускорение получит шаттл;
    - считай, что расход единицы топлива происходит мгновенно, после чего на одну секунду шаттл получает соответствующее его текущей (без учета только что потраченного топлива) массе ускорение;
    - на последней секунде торможения двигатели могут работать меньше секунды, чтобы не придать шаттлу скорость в обратном направлении; при этом все равно тратится полная единица топлива и ровно секунда на выполнение торможения (оставшееся время шаттл неподвижен).
    
## Гравитационный маневр
Вместо прямого полета можно использовать систему космических тел (звезд, планет и лун) для гравитационных маневров. Каждый маневр дает прирост скорости, но требует точного расчета траектории и затрат топлива на коррекцию курса.

Система космических тел образует ориентированный граф, в котором узлы - это звезды и планеты, а ребра - допустимые прямые перелеты между телами. Движение возможно только по ребрам графа (нельзя произвольно менять курс).

Параметры каждого тела:
- уникальный идентификатор;
- масса тела (в условных единицах);
- список допустимых радиусов маневра (в условных единицах).

Параметры каждого ребра:
- идентификаторы начального и конечного тел;
- расстояние между телами (метры).

Механика гравитационного маневра заключается в следующем:
- при пролете вокруг космического тела шаттл разгоняется на определенную величину, зависящую от таких параметров как, например, радиус облета; соответственно, для каждого тела может быть спланировано несколько различных маневров, но выполнить при пролете мимо тела можно только один;
- появляется дополнительный расход топлива на коррекцию траектории;
- затрачивается дополнительное время на выполнение маневра.

## Что нужно сделать в этой задаче
Написать код, который:
- определит, возможно ли преодолеть найденный путь с имеющимися ресурсами (запасами топлива и воздуха);
- найдет путь в графе космических тел между заданной парой звезд, при котором суммарное время полета будет минимально возможным в условиях ограниченности запасов топлива;
- для каждого тела на пути выберет оптимальный гравитационный маневр. При этом допустимо пролететь мимо тела и не выполнять гравитационный маневр (простое перемещение по графу, затраты времени и топлива на корректировку курса к следующему телу в таком случае считать нулевыми).

## API-интерфейс задачи
Все три задания в рамках Проектного этапа должны составлять **ОДНО** веб-приложение. Приложение должно запускать HTTP-сервер на порту 8000 и обрабатывать запрос (для задачи 1): ```POST /api/v1/robinson_cruise```

Формат запроса (JSON):
```json
{
  "mass_shuttle": 1000.0,
  "mass_fuel_unit": 1.0,
  "power_per_unit": 1000.0,
  "oxygen_time": 864000,
  "total_fuel": 7000,
  "fuel_consumption": 3.0,
  "bodies": [
    {
      "id": "planet1",
      "gravity_assists": [
        {
          "velocity_gain": 10000.0,
          "fuel_consumption": 40,
          "time_to_execute": 1095
        },
        {
          "velocity_gain": 24000.0,
          "fuel_consumption": 84,
          "time_to_execute": 2026
        }
      ]
    },
    {
      "id": "gas_giant",
      "gravity_assists": []
    }
  ],
  "edges": [
    {
      "from": "start_point",
      "to": "planet1",
      "distance": 2e8
    },
    {
      "from": "planet1",
      "to": "gas_giant",
      "distance": 1e5
    },
    {
      "from": "gas_giant",
      "to": "rescue_point",
      "distance": 4e6
    }
  ]
}
```
Формат ответа (JSON):
```json
{
  "can_reach": true,
  "min_flight_time": 709698.8,
  "route": ["start_point", "planet1", "gas_giant", "rescue_point"]
}
```
Код ответа: ```200 OK```

Формат ответа, если не можем долететь:
```json
{
  "can_reach": false
}
```
Код ответа: ```200 OK```

Формат ответа при вводе некорректного формата или диапазона значений (JSON): 
```json
{
  "status": "incorrect_input"
}
```
Код ответа: ```400 Bad Request```

Где:
- ```mass_shuttle``` — масса шаттла без топлива (кг);
- ```mass_fuel_unit``` — масса единицы топлива (кг);
- ```power_per_unit``` — ускорение, прилагаемое к единице массы за счет расхода одной единицы топлива (кг*м/с²);
- ```oxygen_time``` — время, на которое хватит воздуха (секунды, целое число);
- ```total_fuel``` — общий доступный запас топлива (целые ед.);
- ```fuel_consumption``` — расход топлива – сколько единиц топлива сжигается за вывод единицы массы на орбиту при взлете;
- ```bodies``` – космические тела, _0 <= len(bodies) <= 100_:
  - ```id``` — уникальный идентификатор тела (строка);
  - ```gravity_assists``` — описание допустимых гравитационных маневров: для выполнения маневра потребуется `time_to_execute` секунд и `fuel_consumption` единиц топлива, а прирост скорости составит `velocity_gain` м/с;
  - конечные точки маршрута (start_point и rescue_point) в списке bodies не указываются, для них отсутствуют варианты грав. маневров;
- ```edges``` – ребра:
  - ```from``` — идентификатор начального тела;
  - ```to``` — идентификатор конечного тела;
  - ```distance``` — расстояние между телами (метры);
  - в списке ребер должны хотя бы по одному разу упоминаться стартовая и конечная вершины;
- ```can_reach``` — можем долететь или нет (булевое значение);
- ```route``` — последовательность имен космических тел в выбранном пути, начиная со входной и заканчивая выходной;
- ```min_flight_time``` — общее время полета (в секундах, одна цифра после запятой);
- ```status``` — статус ответа приложения. 

Учитывай, что важной частью решения задачи является проверка валидности входных данных. В числе возможных ошибок может быть как несоблюдение описанного выше формата (например, отсутствие или неправильное наименование части полей, ошибки в типах данных), так и некорректность значений (например, выход за допустимые диапазоны, нарушение связности данных или несоответствие условиям задачи).  
Автоматическая проверка включает не только тесты на корректную обработку валидных запросов, но и тесты на некорректные входные данные.  
Решение должно корректно обрабатывать такие случаи и возвращать ответ строго в указанном формате.

За решение этой задачи можно получить максимум **25** баллов.

# Задача 2: «Гори, гори, моя звезда»
## Легенда
Корабль пытается установить стабильную связь с удаленной звездой, которая передает критически важные данные для спасения миссии. Однако сигнал передается очень медленно и требует непрерывного приема в течение длительного времени. 

Проблема: в системе существует сложная иерархия небесных тел (планеты и их спутники), которые постоянно заслоняют собой звезду. Мы не можем позволить себе потерять ни секунды передачи – любой перерыв сделает полученные данные бесполезными. 

Передача данных возможна только при условии прямой видимости звезды на протяжении всего сеанса связи. Твоя задача – найти ближайший временной промежуток, пригодный для передачи, определить момент его наступления и продолжительность. Если таких окон нет — сообщить об этом. Более того, отправка сообщений автоматизируется бортовым компьютером, и потому начать отправку возможно только в начале каждой секунды (т.е., например, можно начать сеанс связи ровно в момент времени `12:40:55`, без милли- или микросекунд на таймере).

## Что мы знаем:
- мы находимся в фиксированной точке наблюдения (точка 0,0 на плоскости) на планете ```Atlas```;
- интересующая нас звезда находится очень далеко — считаем ее материальной точкой;
- вокруг планеты, на которой мы находимся, (вокруг 0,0) располагаются небесные тела – звезды, планеты и луны, образующие иерархическую структуру:
  - звезды неподвижны;
  - планеты вращаются вокруг звезд (движение по круговой орбите);
  - луны вращаются вокруг планет (движение по круговой орбите);
- все небесные тела — это идеальные сферы определенного радиуса, которые могут заслонять нужную нам звезду;
- сама планета, на которой мы находимся, звезду не заслоняет, однако луны (указываются в списке небесных тел с ```parent_id: "Atlas"```), вращающиеся вокруг нее, могут перекрывать нужную нам звезду.

## Звездная система
Особенности, которые стоит учитывать:
- у нас многоуровневая система – тела движутся по сложным траекториям с учетом иерархии;
- сигнал требует непрерывного приема в течение времени `required_transmission_time`;
- мы начинаем наблюдать в момент start_observation_time.

## Что нужно сделать в этой задаче
Написать код, который:
- смоделирует движение всех тел в иерархической системе;
- определит моменты, когда луч от наблюдателя к звезде пересекает какое-либо тело;
- найдет интервалы времени, когда звезда полностью видна;
- найдет ближайший к началу наблюдения интервал длиной не менее ```required_transmission_time```;
- вернет время ожидания ближайшего подходящего интервала и длину этого интервала (количество целых секунд, когда звезда видна).

Например, мы начинаем наблюдение за звездой в момент времени 0 и нам необходимо 100 секунд для передачи сигнала, а ближайшие два затмения занимают следующие временные интервалы: `[35.1, 74.2]`, `[180.5, 197.8]`, то ближайшим подходящим нам интервалом будет `[75, 180)`, т.е. придется подождать 75 секунд, а сам интервал будет иметь длину 105.


## API-интерфейс задачи
Напоминаем, что все три задания в рамках Проектного этапа должны составлять **ОДНО** веб-приложение. Приложение должно запускать HTTP-сервер на порту 8000 и обрабатывать запрос (для задачи 2): ```POST /api/v1/star_visibility```

Формат запроса (JSON):
```json
{
  "target_star_vector": {"x": 1.0, "y": 0.0},
  "celestial_bodies": [
    {
      "type": "star",
      "id": "star_alpha",
      "position": {"x": 5.0e10, "y": 6.5e9},
      "radius": 6.0e9
    },
    {
      "type": "planet",
      "id": "planet_1",
      "parent_id": "star_alpha",
      "orbit_radius": 3.0e10,
      "angular_velocity": 2.0e-3,
      "initial_angle": 150.0,
      "radius": 4.0e6,
      "rotation_clockwise": false
    },
    {
      "type": "moon",
      "id": "moon_1",
      "parent_id": "planet_1",
      "orbit_radius": 2.0e9,
      "angular_velocity": 7.2e-3,
      "initial_angle": 90.0,
      "radius": 1.5e6,
      "rotation_clockwise": true
    }
  ],
  "observation_params": {
    "start_time": "2026-03-21T00:00:00Z",
    "required_transmission_time": 70000
  }
}
```
Формат ответа, если окно найдено (JSON):
```json
{
  "found": true,
  "next_fitting_interval_in": 21261,
  "interval_duration": 75655
}
```
Код ответа: ```200 OK```

Формат ответа, если окно не найдено (JSON):
```json
{
  "found": false
}
```
Код ответа: ```200 OK```

Формат ответа при вводе некорректного формата или диапазона значений (JSON):
```json
{
  "status": "incorrect_input"
}
```
Код ответа: ```400 Bad Request```

Где:
- ```target_star_vector``` — координаты вектора, задающего направление на звезду, за которой мы наблюдаем;
- ```celestial_bodies``` — небесные тела, _0 <= len(celestial_bodies) <= 100_:
  - ```type``` — тип тела: "star" (неподвижна), "planet", "moon";
  - ```id``` — уникальный идентификатор;
  - Для звёзд:
    - ```position``` — фиксированные координаты звезды (`x` и `y`, метры);
    - ```radius``` — физический радиус тела (метры);
  - Для планет и лун:
    - ```parent_id``` — идентификатор родительского тела (звезды для планет, планеты для лун); помни, что вокруг планеты *Atlas*, на которой мы находимся, тоже могут вращаться луны;
    - ```orbit_radius``` — радиус орбиты (метры);
    - ```angular_velocity``` — угловая скорость (градусы в секунду, значение > 0);
    - ```initial_angle``` — начальный угол на орбите на момент времени `start_time` (градусы, относительно положительной оси X в направлении против часовой стрелки, _0 <= initial_angle < 360_);
    - ```radius``` — физический радиус тела (метры);
    - ```rotation_clockwise``` - направление, в котором планета вращается по орбите (по или против часовой стрелки).
- ```observation_params``` — параметры наблюдения:
  - ```start_time``` — время начала наблюдения (строка в формате ISO 8601: "ГГГГ-ММ-ДДTчч:мм:ссZ");
  - ```required_transmission_time``` — минимальное непрерывное время видимости (X, секунды), _1 <= required_transmission_time <= 1e6_;
- ```found``` — найдено ли окно видимости (true/false);
- ```next_fitting_interval_in``` — сколько ждать следующий интервал (в секундах); гарантируется, что при существовании интервала требуемого размера время ожидания не превышает _1e9_ секунд;
- ```interval_duration``` — длительность интервала (в секундах); выведите “inf”, если целевая звезда никогда не закрывается заданной конфигурацией звезд, планет и лун;
- ```status``` — статус ответа приложения. 

Учитывай, что важной частью решения задачи является проверка валидности входных данных. В числе возможных ошибок может быть как несоблюдение описанного выше формата (например, отсутствие или неправильное наименование части полей, ошибки в типах данных), так и некорректность значений (например, выход за допустимые диапазоны, нарушение связности данных или несоответствие условиям задачи).  
Автоматическая проверка включает не только тесты на корректную обработку валидных запросов, но и тесты на некорректные входные данные.  
Решение должно корректно обрабатывать такие случаи и возвращать ответ строго в указанном формате.

За решение этой задачи можно получить максимум **50** баллов.

# Задача 3: «Звездочетность»
## Легенда

Корабль потерял ориентацию в пространстве. Чтобы определить свое местоположение в галактике и построить обратный курс, нужно найти на небе знакомое созвездие — тот самый астронавигационный маркер, по которому можно сориентироваться.

Проблема в том, что мы не знаем, в какой части галактики находимся, и поэтому созвездие, которое мы ищем, может быть произвольным образом искажено в пространстве. Полагаться на визуальное сходство нельзя — нужно найти похожую по структуре конфигурацию звезд.

Под похожей конфигурацией пары созвездий мы будем понимать совпадение сразу двух свойств графов, которые задают созвездия:
- графы должны быть изоморфны друг другу;
- мы должны определить взаимно-однозначное соответствие между вершинами (звездами) первого и второго созвездий, и для этого потребуется проанализировать длины ребер: они могут отличаться в силу пространственных искажений, однако относительный порядок сохраняется. 

Например, мы наблюдаем структуру `a - (10) - b - (20) - c` на первом графе и `x - (5) - y - (50) - z` на втором (в скобках обозначены длины ребер). Мы понимаем, что эти два графа изоморфны друг другу, а по относительной величине ребер (ребро `xy` меньше ребра `yz`, как и ребро `ab` меньше, чем `bc`) можно сделать вывод, что мы можем сопоставить звезды так: `a->x`, `b->y`, `c->z`.

## Что мы знаем
Нам повезло! Местные жители предоставили доступ к своему звездному справочнику (Star Catalog). В нем для каждой видимой звезды записаны:
- ее уникальное местное имя (строка);
- координаты (x, y, z) в системе координат данной планеты.

Но имена в справочнике совершенно незнакомые, а координаты привязаны к этой планете, поэтому напрямую сопоставить их с нашими картами нельзя. Однако мы знаем, что местные жители строят созвездия по единым общегалактическим правилам:
1. Звезды делятся на группы (созвездия) по пространственной близости, а именно: группой считается компонента связности неориентированного графа, рёбра которого проведены между звездами, находящимися на расстоянии, не превышающем заданное значение D (параметр `max_neighbor_distance`).
Фактически это означает, что созвездие является максимальным по включению подмножеством звезд, между любыми двумя вершинами из которого существует маршрут, причем ни одно расстояние между парой звезд в маршруте не превышает D.

2. В созвездии должно быть не меньше `min_size` и не больше `max_size` звёзд. 
Одиночные звезды, а также слишком маленькие или слишком большие скопления созвездиями не считаются.

3. Сама схема созвездия (какие звезды соединены линиями) строится как минимальное остовное дерево (MST) на множестве его звёзд.
Сумма длин ребер в этом дереве минимальна, циклы отсутствуют.
Гарантируется, что для каждого созвездия минимальное остовное дерево единственно (все попарные расстояния между звездами одной группы различны).

## Что нужно сделать в этой задаче
Написать код, который:
- кластеризует звезды по правилу связности с порогом D и отбирает только группы, чей размер попадает в интервал [`min_size`, `max_size`]; полученные группы считаются наблюдаемыми созвездиями;
- для каждого такого созвездия восстанавливает его схему — минимальное остовное дерево, используя евклидовы расстояния между звёздами;
- сравнивает схемы всех найденных созвездий с заданным графом целевого созвездия на схожесть структуры (изоморфизм и соответствие звезд по длинам ребер);
- если ровно одно созвездие на небе оказалось похожим на целевое, возвращает список имен звезд, соответствующих вершинам целевого графа (в порядке вершин 0…n-1); в противном случае (ни одного или несколько совпадений) возвращает ```found: false```.

## API-интерфейс задачи
Напоминаем, что все три задания в рамках Проектного этапа должны составлять **ОДНО** веб-приложение. Приложение должно запускать HTTP-сервер на порту 8000 и обрабатывать запрос (для задачи 3): ```POST /api/v1/constellation_finder```

Формат запроса (JSON):
```json
{
  "stars": [
    {"name": "star_1", "x": 1.2e6, "y": 12.5e7, "z": 9.0e8},
    {"name": "star_2", "x": 2.3e6, "y": -1.2e6, "z": 9.3e8},
    ...
  ],
  "cluster_params": {
    "min_size": 3,
    "max_size": 10,
    "max_neighbor_distance": 1e10
  },
  "target_constellation": {
    "edges": [
      {"from": 0, "to": 1, "distance": 4e8},
      {"from": 1, "to": 2, "distance": 2e9},
      {"from": 2, "to": 3, "distance": 3e9}
    ]
  }
}
```
Формат ответа (JSON):
```json
{
  "found": true,
  "matched_stars": ["star_1", "star_17", "star_18", "star_4"]
}
```
Код ответа: ```200 OK```

Формат ответа (JSON), если ни одного созвездия, изоморфного целевому, не найдено или таких созвездий несколько:
```json
{
  "found": false
}
```
Код ответа: ```200 OK```

Формат ответа при вводе некорректного формата или диапазона значений (JSON):
```json
{
  "status": "incorrect_input"
}
```
Код ответа: ```400 Bad Request```

Где:
- ```stars``` — массив объектов, описывающих все видимые звезды; _0 <= len(stars) <= 1000_; каждый объект содержит:
  - ```name``` — уникальное строковое имя звезды (местный идентификатор);
  - ```x```, ```y```, ```z``` — трехмерные координаты звезды (числа с плавающей запятой);
- ```cluster_params``` — параметры кластеризации:
  - ```min_size``` — минимальное допустимое количество звезд в созвездии (X);
  - ```max_size``` — максимальное допустимое количество звезд в созвездии (Y);
  - ```max_neighbor_distance``` — максимальное расстояние до ближайшего соседа внутри созвездия (D);
- ```target_constellation``` — описание искомого созвездия (граф из n вершин с индексами [0…n-1], _2 <= n <= 50_); описание представляет собой список ребер, каждое из которых задается индексами соединенных вершин и длиной ребра; гарантируется, что граф не имеет кратных ребер и петель, однако следует убедиться в том, что он представляет собой корректное остовное дерево;
- ```matched_stars``` — массив имен звезд из входного списка, соответствующих вершинам целевого созвездия (в порядке от 0 до n-1);
- ```status``` — статус ответа приложения. 

Учитывай, что важной частью решения задачи является проверка валидности входных данных. В числе возможных ошибок может быть как несоблюдение описанного выше формата (например, отсутствие или неправильное наименование части полей, ошибки в типах данных), так и некорректность значений (например, выход за допустимые диапазоны, нарушение связности данных или несоответствие условиям задачи).  
Автоматическая проверка включает не только тесты на корректную обработку валидных запросов, но и тесты на некорректные входные данные.  
Решение должно корректно обрабатывать такие случаи и возвращать ответ строго в указанном формате.

За решение этой задачи можно получить максимум **75** баллов.


**Удачи в решении задач! Твои алгоритмы помогут экипажу найти путь домой!**

# Технические требования для всех задач

## 1. Требования к репозиторию

1. Перед выполнением задач тебе необходимо сделать форк текущего репозитория (как это сделать, мы рассказывали в вводном туре по GitVerse).
2. Твой репозиторий (форк текущего) должен быть приватным, однако необходимо добавить нашего проверяющего - аккаунт ```it-marathon``` - в соавторы с доступом админа (сделать это можно в настройках репозитория).
3. После выполнения заданий твой код и сопутствующие файлы должны быть перенесены на ветку **develop** (именно она будет проверяться).
4. Перед отправкой убедись, что последний коммит содержит финальную версию.
5. Все значимые изменения кода должны сопровождаться коммитами. История коммитов должна быть логичной и отражать процесс разработки.
6. По выполнении заданий у твоего репозитория должна получиться следующая структура:
```
├── src/                   # Основная папка с твоим решением
├── .cm-token              # Тут должен быть твой токен (см. п. 3)
├── instruction.md         # Инструкция по сборке (см. п. 5)
├── Dockerfile
├── docker-compose.yml
└── README.md              # Текущий файл с описанием задач
```
Напоминаем, что выполнение заданий должно быть самостоятельным. Если в репозитории будет зафиксировано добавление других участников (кроме аккаунта проверяющего) или параллельные коммиты с разных аккаунтов, мы будем вынуждены тебя дисквалифицировать согласно п. 9.5.5 [Положения о конкурсе](https://it-marathon.21-school.ru/clause.pdf).

## 2. Требования к API
Все 3 задания должны составлять **ОДНО** веб-приложение. Приложение должно запускать HTTP-сервер на порту 8000 и обрабатывать следующие запросы:
- эндпоинт для задачи 1: ```POST /api/v1/robinson_cruise```
- эндпоинт для задачи 2: ```POST /api/v1/star_visibility```
- эндпоинт для задачи 3: ```POST /api/v1/constellation_finder```

Ответы API должны строго соответствовать указанным форматам.  
Добавление дополнительных полей, не предусмотренных спецификацией, не допускается.

## 3. Токен доступа
Для того, чтобы мы смогли идентифицировать твой репозиторий, тебе необходимо добавить в файл  ```.cm-token``` свой токен, который ты можешь найти в Личном кабинете в разделе "Профиль" либо на карточке этапа.

Файл должен содержать только твой токен (одна строка) и ничего больше.

Пример: ```a1b2c3d4-e5f6-4a1b-8c2d-e3f4a5b6c7d8```

## 4. Правила реализации
Решение может быть представлено на любом языке программирования, который позволит решить указанные задачи. 

Однако версии языков должны быть обязательно не менее указанных:
- Python 3.14
- Java JDK 25 (LTS)
- JavaScript ES2025 (ECMAScript 2025)
- TypeScript 5.8
- C# 14
- Go 1.25
- Rust 1.94
- C++ 23
- PHP 8.3
- Ruby 3.3
- R 4.4
- C 23

Если твоего языка нет в списке – используй последнюю актуальную версию.

Важно: используемое дополнительное программное обеспечение (библиотеки, фреймворки, подключаемые программные модули), при наличии такой возможности, должно быть отечественным, свободно распространяемым или бесплатным.

**Ограничения:**
- общий вес репозитория не должен превышать **64 Мбайт**;
- скорость ответа на запрос должна быть **не более 3 секунд**;
- запрещено менять порт внутри контейнера (должен быть 8000);
- запрещено использовать внешние сетевые вызовы (кроме localhost);
- решение должно потреблять не более **1 Гбайт** оперативной памяти в собранном виде.

## 5. Сборка и запуск

Твое приложение должно:
- запускаться на ```0.0.0.0:8000```;
- обрабатывать **все три эндпоинта** задач.

Решение должно быть представлено **в виде ссылки на репозиторий GitVerse**, содержащий следующие артефакты:
- исходный код решения с инструкцией по ручному запуску (для случаев, когда будет нужна перепроверка экспертом вручную);
- ```Dockerfile``` для автоматической сборки образа;
- ```docker-compose.yml```, ссылающийся на готовый Docker-образ, опубликованный тобой в GitVerse Registry.

В случае затруднений обращайся к официальной документации [GitVerse](https://gitverse.ru/docs/).

Пройдемся по всем артефактам подробно. 

### Инструкция по ручному запуску

В файле ```instruction.md``` опиши пошаговую процедуру запуска твоего приложения для ручной проверки. Укажи:
- необходимые зависимости и команды для их установки;
- команды для сборки;
- команду для запуска сервера;
- как проверить работоспособность;
- другие процедуры, необходимые для запуска именно твоего решения.

Важно: опиши все максимально подробно!

### Dockerfile
Тебе необходимо создать ```Dockerfile```, который собирает образ с твоим решением. При проверке автоматическая тестовая система выполнит команду ```docker build .``` **из корня репозитория**  и запустит полученный образ, ожидая работающий сервер на порту 8000.

**Требования к Dockerfile:**
- базовый образ должен быть взят из зеркала GitVerse Docker Hub;
- установка зависимостей должна производиться внутри образа.

Обязательно проверь ```Dockerfile``` перед отправкой: собери образ локально и запусти контейнер. Его поведение должно соответствовать твоим ожиданиям и заданию, иначе весь труд будет потрачен впустую.

Файл должен называться точно ```Dockerfile```, находиться в **корне репозитория** и корректно собираться командой ```docker build .```

### Docker Compose со ссылкой на образ в GitVerse Registry

Тебе необходимо самостоятельно собрать образ своего решения, опубликовать его в GitVerse Registry, а затем указать ссылку на этот образ в файле ```docker-compose.yml```.

Для этого тебе необходимо:
- авторизоваться в GitVerse Registry;
- собрать docker-образ;
- запушить собранный образ в GitVerse Registry;
- в корне репозитория создать файл ```docker-compose.yml``` с этим образом;
- проверить работоспособность compose-файла.

Важно: перед сдачей решения проверь, что:
- все файлы (```instruction.md```, ```Dockerfile```, ```docker-compose.yml```) находятся в корне репозитория;
- образ в GitVerse Registry доступен.

Файл ```docker-compose.yml``` должен содержать описание **ровно одного сервиса** — сервиса с твоим приложением.


Ты несешь ответственность за тот код и содержимое образа, которые ты направляешь на проверку, в соответствии с [Положением о конкурсе](https://it-marathon.21-school.ru/clause.pdf), и нарушение (в том числе неправомерные действия) приведет к дисквалификации.

## 6. Процедура сдачи решения

После выполнения заданий тебе необходимо загрузить их в Личный кабинет (ЛК). Алгоритм действий:
1. открыть карточку проектного этапа в ЛК;
2. вставить в поле ввода ссылку на репозиторий;
3. нажать «Валидировать решение» – будет произведена проверка на соответствие репозитория правилам загрузки решения;
4. при успешной проверке нажать «Сдать задание»;
5. если при валидации обнаружились ошибки, необходимо их исправить и пройти валидацию заново, нажав «Валидировать решение».

Обращаем внимание, что задание можно сдать только один раз, поэтому перед отправкой обязательно перепроверь свою работу. 

> Важно: при проверке твоей работы автоматической системой будет оцениваться последний коммит (на момент загрузки решения) в ветке **develop**.

## 7. Сроки
Решение должно быть сдано **до 23:59 (по московскому времени) 20 апреля 2026 года**.

> Напоминаем, что публикация решения в открытых источниках (включая GitHub, GitLab и другие публичные репозитории) запрещена. Нарушение данного требования влечет дисквалификацию в соответствии с п. 9.5.9 [Положения о конкурсе](https://it-marathon.21-school.ru/clause.pdf)

Если у тебя возникли сложности, обращайся в поддержку через Личный кабинет.